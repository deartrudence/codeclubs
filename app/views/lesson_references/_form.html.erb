<%= simple_form_for(@lesson_reference) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :url %>
    <%= f.association :lesson %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
